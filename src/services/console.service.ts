// Maybe this should be a controller...

const ansiEscapes = require("ansi-escapes");
const write = process.stdout.write.bind(process.stdout); //Similar to console.log()

export class ConsoleService {}
